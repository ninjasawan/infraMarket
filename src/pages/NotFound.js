import React from "react";

const NotFound = () => {
  return (
    <h1
      style={{
        minHeight: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "red",
      }}
    >
      Page not found!!!
    </h1>
  );
};

export default NotFound;
